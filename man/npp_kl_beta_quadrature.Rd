% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/npp_kl_calibration.R
\name{npp_kl_beta_quadrature}
\alias{npp_kl_beta_quadrature}
\title{Quadrature rule for a Beta measure on the unit interval}
\usage{
npp_kl_beta_quadrature(alpha_gamma, beta_gamma, n_nodes = 80L)
}
\arguments{
\item{alpha_gamma}{First shape parameter of the Beta prior.}

\item{beta_gamma}{Second shape parameter of the Beta prior.}

\item{n_nodes}{Number of quadrature nodes.}
}
\value{
A list with \code{nodes}, strictly inside the unit interval, and
\code{log_weights}, the log of the Beta measure each node carries.
}
\description{
The calibration integrates against \code{Beta(a, b)} densities whose
shape parameters go as low as 0.05, so the integrand has an algebraic
singularity at each endpoint. Gauss-Jacobi integrates
\eqn{\gamma^{a-1}(1-\gamma)^{b-1}} exactly, leaving only the smooth remainder
to the rule, which is the same argument \code{\link[=npp_prior_mixture]{npp_prior_mixture()}} makes for the
prior mixture. Substituting \eqn{\gamma = (1 + x)/2} maps
\code{statmod::gauss.quad()}'s interval onto the unit interval and turns its weight
function \eqn{(1-x)^\alpha (1+x)^\beta} into the Beta kernel with
\eqn{\alpha = b - 1} and \eqn{\beta = a - 1}, at the cost of the constant
\eqn{2^{1-a-b}}.

Dividing that by the Beta function leaves weights that are the Beta measure
itself, so they sum to one; they are returned on the log scale because the
likelihood they are combined with is evaluated there.
}
\keyword{internal}
