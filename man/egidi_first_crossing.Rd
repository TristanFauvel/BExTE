% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vectorised_egidi_mixture.R
\name{egidi_first_crossing}
\alias{egidi_first_crossing}
\title{First candidate weight at which the conflict criterion is met}
\usage{
egidi_first_crossing(lower, upper, steps, evaluate, alpha_pc)
}
\arguments{
\item{lower, upper}{Per-replicate ends of the interval to scan.}

\item{steps}{Number of equal steps the interval is cut into.}

\item{evaluate}{Function of a candidate weight and the row indices it belongs
to, returning the conflict p-value.}

\item{alpha_pc}{Conflict threshold.}
}
\value{
A list with the bracketing \code{lower} and \code{upper} weights.
}
\description{
Scans a grid upwards and returns, for each replicate, the
interval in which the criterion is first satisfied.
}
\details{
The scan runs upwards and stops at the first crossing rather than searching the
whole interval, because the quantity wanted is an infimum. A bisection over
\eqn{[0, 1]} would be wrong here: the conflict p-value is not monotone in the
weight when the components have different centres, and it can fall before it
rises when the observed statistic sits near the informative component's centre.

Replicates leave the scan as they resolve, so later steps are evaluated only
for those still looking.
}
\keyword{internal}
