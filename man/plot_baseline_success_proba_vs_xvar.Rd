% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_success_probability_vs_scenario.R
\name{plot_baseline_success_proba_vs_xvar}
\alias{plot_baseline_success_proba_vs_xvar}
\title{Add a reference probability of success to a metric-vs-x-variable plot}
\usage{
plot_baseline_success_proba_vs_xvar(
  plt,
  data,
  xvar_name,
  selected_metric_name,
  cap_size,
  markersize,
  join_points,
  label = NULL,
  selected_metric_uncertainty_lower = NULL,
  selected_metric_uncertainty_upper = NULL
)
}
\arguments{
\item{plt}{The plot to add the baseline to.}

\item{data}{The dataframe holding the baseline.}

\item{xvar_name}{Name of the x-axis variable.}

\item{selected_metric_name}{Name of the column holding the baseline.}

\item{cap_size}{Width of the error bar caps.}

\item{markersize}{Size of the points.}

\item{join_points}{Whether to join the points with a line.}

\item{label}{Legend label for the baseline.}

\item{selected_metric_uncertainty_lower}{Name of the column holding the
lower confidence bound, or \code{NULL} for no error bars.}

\item{selected_metric_uncertainty_upper}{Name of the column holding the
upper confidence bound, or \code{NULL} for no error bars.}
}
\value{
The plot, with the baseline added.
}
\description{
Draws the baseline as points, optionally joined by a line.
When \code{selected_metric_uncertainty_lower}/\verb{_upper} name columns that carry
a non-degenerate interval, the baseline also gets error bars.

The bounds are only informative where the power was approximated by Monte
Carlo: \code{compute_freq_power()} returns an exact binomial interval when it
simulates, but a degenerate \code{c(power, power)} when it has a closed form.
Handing those degenerate bounds to \code{geom_errorbar()} would draw a
zero-height bar - a bare cap tick - through every marker, so they are
left off. Results that predate the bound columns are treated the same
way, which keeps their figures rendering.
}
\keyword{internal}
