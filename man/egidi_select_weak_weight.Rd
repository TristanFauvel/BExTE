% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vectorised_egidi_mixture.R
\name{egidi_select_weak_weight}
\alias{egidi_select_weak_weight}
\title{Select the smallest acceptable weak-component weight}
\usage{
egidi_select_weak_weight(
  t_obs,
  s_target,
  mu_p,
  tau_p,
  mu_q,
  tau_q,
  alpha_pc = 0.05,
  weight_grid_step = 0.001,
  weight_scan_step = 0.02,
  refinements = 30L
)
}
\arguments{
\item{t_obs}{Observed target treatment effect estimate, one per replicate.}

\item{s_target}{Target standard error, one per replicate.}

\item{mu_p, tau_p}{Informative component prior mean and standard deviation.}

\item{mu_q, tau_q}{Weak component prior mean and standard deviation.}

\item{alpha_pc}{Conflict threshold, 0.05 in the primary analysis.}

\item{weight_grid_step}{Resolution of the weight scan.}

\item{weight_scan_step}{Resolution of the coarse stage of the scan.}

\item{refinements}{Number of bisection steps used to refine the crossing.}
}
\value{
A data frame with one row per replicate and the columns \code{psi_weak},
\code{pvalue_informative}, \code{pvalue_weak}, \code{pvalue_selected}, \code{initial_conflict}
and \code{conflict_unresolved}.
}
\description{
Implements the Egidi, Pauli and Torelli selection rule
\deqn{\hat\psi = \inf\{\psi \in [0, 1] : P_\psi(t_{obs}) \ge \alpha_{PC}\},}
separately for every replicate and using only the observed target statistic.
}
\details{
Both ends of the interval are exact and are tried first, which decides most
replicates without any search. \eqn{P_0} is the conflict p-value under the
informative component alone: when it already reaches the threshold there is no
conflict to resolve and the selected weight is zero. \eqn{P_1} is the conflict
p-value under the weak component alone: when even that falls short, no weight
removes the conflict, and the rule returns one while flagging the conflict as
unresolved rather than treating it as resolved.

With common centres the mixture predictive is symmetric, \eqn{P_\psi} is exactly
\eqn{(1 - \psi)P_p + \psi P_q}, and the crossing is solved in closed form.
Otherwise the weight is found by scanning upwards for the first crossing and
refining it, since monotonicity in \eqn{\psi} is not guaranteed.

The scan is run in two stages, a coarse one to locate the crossing and a fine
one at \code{weight_grid_step} inside it, and then bisected. The two-stage scan
agrees with a single scan at \code{weight_grid_step} unless a crossing both starts
and ends inside one coarse step; \code{weight_scan_step = weight_grid_step} disables
the coarse stage.
}
\keyword{internal}
