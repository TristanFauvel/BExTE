% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/npp_kl_calibration.R
\name{npp_kl_calibration_columns}
\alias{npp_kl_calibration_columns}
\title{The calibration columns reported with every replicate}
\usage{
npp_kl_calibration_columns(calibration, n_replicates)
}
\arguments{
\item{calibration}{A list from \code{\link[=calibrate_npp_kl]{calibrate_npp_kl()}}.}

\item{n_replicates}{Number of rows to produce.}
}
\value{
A data frame of \code{n_replicates} identical rows.
}
\description{
These are constant within a scenario: the prior is calibrated
once from the design and reused for every replicate. They are repeated for
each replicate anyway because the reporting layer averages every column of
the posterior parameters over the replicates
(\code{estimate_frequentist_operating_characteristics()}), so a constant column
arrives in the results as that constant.

\code{calibration_id} is deliberately absent: that averaging would turn a
character column into \code{NA}. The calibration unit is identifiable in the
results from \code{alpha_gamma}, \code{beta_gamma}, \code{d_mtd} and \code{se_target_expected}.
}
\keyword{internal}
