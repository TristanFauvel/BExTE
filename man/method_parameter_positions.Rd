% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots_utils.R
\name{method_parameter_positions}
\alias{method_parameter_positions}
\title{Where each parameter label sits on its method's ramp}
\usage{
method_parameter_positions(key, parameter_labels, dict)
}
\arguments{
\item{key}{The method key.}

\item{parameter_labels}{The labels to place, as plain text.}

\item{dict}{The run's methods configuration.}
}
\value{
A numeric vector of positions in \link{0, 1}.
}
\description{
Anchored on the range the configuration declares rather than on
the values one figure happens to show. A figure plotting w in {0.1, 0.5,
0.9} and one plotting all nine weights put w = 0.5 at the same place, and
so give it the same shade; ranking the values present would not.

Labels carrying no recoverable number - categorical parameters - fall back
to sorted order, which is at least reproducible between runs.
}
\keyword{internal}
