% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/npp_kl_calibration.R
\name{npp_kl_optimise_from}
\alias{npp_kl_optimise_from}
\title{One run of the calibration optimiser}
\usage{
npp_kl_optimise_from(start, objective, beta_parameter_bounds)
}
\arguments{
\item{start}{Length-two numeric vector of starting shape parameters, on the
natural scale.}

\item{objective}{A function of \code{eta}, as \code{\link[=npp_kl_objective]{npp_kl_objective()}}.}

\item{beta_parameter_bounds}{Bounds on the shape parameters.}
}
\value{
A list with \code{alpha_gamma}, \code{beta_gamma}, \code{objective_value},
\code{converged} and \code{message}. A start the optimiser cannot use at all is
reported as not converged with an infinite objective rather than raised.
}
\description{
Factored out of \code{\link[=calibrate_npp_kl]{calibrate_npp_kl()}} so that a single starting
value can be exercised, and made to fail, on its own.

L-BFGS-B works on the log shapes inside the log of the configured bounds,
which is why the bounds are guaranteed to hold exactly rather than
approximately. It is deterministic, so two calibrations with the same inputs
return the same answer.
}
\keyword{internal}
