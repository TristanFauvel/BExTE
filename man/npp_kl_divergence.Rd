% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/npp_kl_calibration.R
\name{npp_kl_divergence}
\alias{npp_kl_divergence}
\title{Kullback-Leibler divergence from a discretised posterior to a Beta reference}
\usage{
npp_kl_divergence(posterior, reference_shape1, reference_shape2)
}
\arguments{
\item{posterior}{A list from \code{\link[=npp_kl_posterior_masses]{npp_kl_posterior_masses()}}.}

\item{reference_shape1}{First shape parameter of the reference Beta.}

\item{reference_shape2}{Second shape parameter of the reference Beta.}
}
\value{
A single number, or \code{Inf} when the integrand is not finite anywhere
the posterior puts mass.
}
\description{
Computes \eqn{\int_0^1 p(\gamma)[\log p(\gamma) - \log q(\gamma)]
d\gamma} with \code{p} the hypothetical posterior and \code{q} one of the two reference
Beta distributions. The integral is taken in the direction written here, not
the reverse one: it penalises posterior mass placed where the reference has
little, which is what "the posterior should look like full borrowing" means.

Quadrature enters through the posterior masses rather than through a separate
set of weights, because those masses already carry the rule.
}
\keyword{internal}
