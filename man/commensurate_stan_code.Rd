% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/method_power_priors.R
\name{commensurate_stan_code}
\alias{commensurate_stan_code}
\title{Stan program for the commensurate prior, with or without a power parameter}
\usage{
commensurate_stan_code(with_power_parameter)
}
\arguments{
\item{with_power_parameter}{Whether the program samples the power parameter.}
}
\value{
The Stan program, as a single string.
}
\description{
The two commensurate methods fit the same model up to one
term. The commensurate power prior of Hobbs et al. (2011) samples a power
parameter \code{gamma ~ Beta(g(tau), 1)} alongside the commensurability
precision; the plain commensurate prior is its \code{gamma == 1} case, in which
every product \code{gamma * NS} collapses to \code{NS} and the Beta statement and
\code{g_function} fall away. Holding both programs here keeps that relationship
visible.

The \code{data} block is deliberately identical in the two, so a single
\code{prepare_data()} serves both classes; Stan accepts data a program does not
read. \code{test-commensurate-stan-model.R} checks that the two blocks have not
drifted apart.
}
\keyword{internal}
