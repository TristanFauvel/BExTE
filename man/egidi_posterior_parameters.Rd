% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/method_egidi_mixture.R
\name{egidi_posterior_parameters}
\alias{egidi_posterior_parameters}
\title{Per-replicate diagnostics reported by the Egidi mixture prior}
\usage{
egidi_posterior_parameters(informative_posterior_weight, selection)
}
\arguments{
\item{informative_posterior_weight}{Posterior probability of the informative
component.}

\item{selection}{A data frame from \code{\link[=egidi_select_weak_weight]{egidi_select_weak_weight()}}.}
}
\value{
A data frame with one row per replicate.
}
\description{
Assembles the columns both the scalar and the vectorised paths
report, so the two cannot describe the same run differently.
}
\details{
The summariser in \code{\link[=Model$estimate_frequentist_operating_characteristics]{Model$estimate_frequentist_operating_characteristics()}}
averages each column, so the quantities that are proportions are reported as
indicators whose mean is the proportion. The spread of the selected weight,
which no mean can give, comes from \code{quantile_summary_columns} instead.

\code{prior_weight} is the posterior probability that the treatment effect came
from the informative component, which is what the robust mixture prior reports
under that name; the figures and tables that read it therefore work unchanged.
It is not the selected prior weight, which is \code{informative_prior_weight}.
}
\keyword{internal}
