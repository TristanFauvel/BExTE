% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/npp_kl_calibration.R
\name{calibrate_npp_kl}
\alias{calibrate_npp_kl}
\title{Calibrate the normalised power prior by a KL criterion}
\usage{
calibrate_npp_kl(
  theta_source,
  se_source,
  se_target_expected,
  theta_null = 0,
  benefit_sign = 1,
  d_mtd = NULL,
  d_mtd_multiplier = 1,
  lambda_kl = 0.5,
  c_target = 10,
  beta_parameter_bounds = NPP_KL_DEFAULT_BOUNDS,
  optimizer_starts = NPP_KL_DEFAULT_STARTS,
  n_nodes = 80L
)
}
\arguments{
\item{theta_source}{Source treatment effect estimate.}

\item{se_source}{Standard error of the source estimate. Must be positive.}

\item{se_target_expected}{Standard error the target design implies for its
treatment effect estimate. Must be positive.}

\item{theta_null}{Boundary of the null hypothesis space, the \code{theta_0} of the
case study.}

\item{benefit_sign}{\code{1} when larger treatment effects are beneficial, \code{-1}
when smaller ones are. \code{\link[=benefit_sign_from_null_space]{benefit_sign_from_null_space()}} derives it from a
case study's \code{null_space}.}

\item{d_mtd}{Maximum tolerable discrepancy. \code{NULL}, the default, applies the
rule \code{d_mtd_multiplier * abs(theta_source - theta_null)}; a number
overrides that rule, and \code{d_mtd_multiplier} is then not applied.}

\item{d_mtd_multiplier}{Multiplier used by the default rule.}

\item{lambda_kl}{Weight on the compatible term, in \verb{[0, 1]}.}

\item{c_target}{Shape of the two reference Beta distributions. Must exceed
one.}

\item{beta_parameter_bounds}{Bounds on the calibrated shape parameters.}

\item{optimizer_starts}{List of length-two starting values, on the natural
scale. The search keeps the converged answer with the smallest objective.}

\item{n_nodes}{Number of Gauss-Jacobi nodes used for every integral.}
}
\value{
A list with the calibrated \code{alpha_gamma} and \code{beta_gamma}, the
\code{objective_value} they attain, \code{optimizer_converged} and
\code{optimizer_message}, the two hypothetical estimates
\code{theta_target_compatible} and \code{theta_target_mtd}, the \code{d_mtd},
\code{d_mtd_multiplier}, \code{lambda_kl}, \code{c_target} and \code{se_target_expected} used,
and a \code{calibration_id} identifying the calibration unit.
}
\description{
Chooses the \code{Beta(a, b)} prior on the discounting parameter of the normalised
power prior so that the prior is informative about \emph{when} to borrow rather
than about \emph{how much}. Two hypothetical target estimates stand for the two
situations the prior has to tell apart:
\describe{
\item{compatibility}{the target estimate falls exactly on the source
estimate, and the discounting parameter should concentrate near one;}
\item{maximum tolerable discrepancy}{the target estimate falls
\code{d_mtd} away from it, towards the null, and the discounting parameter
should concentrate near zero.}
}
Writing \eqn{p_0} and \eqn{p_{MTD}} for the marginal posteriors of the
discounting parameter these two imply, the calibration minimises
\deqn{K(a, b) = \lambda \, \mathrm{KL}[p_0 \| \mathrm{Beta}(c, 1)] +
  (1 - \lambda) \, \mathrm{KL}[p_{MTD} \| \mathrm{Beta}(1, c)].}

Both hypothetical posteriors are formed from the \emph{expected} target standard
error, the one the design implies, not from any realised estimate. The
calibration therefore belongs to the scenario and is computed once for it,
before any replicate is generated; every replicate of that scenario is then
analysed under the same prior, and only the target estimate and its standard
error vary between them.
}
