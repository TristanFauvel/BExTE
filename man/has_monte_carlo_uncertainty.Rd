% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots_utils.R
\name{has_monte_carlo_uncertainty}
\alias{has_monte_carlo_uncertainty}
\title{Whether a pair of bound columns carries Monte Carlo uncertainty}
\usage{
has_monte_carlo_uncertainty(data, lower, upper)
}
\arguments{
\item{data}{The dataframe backing the layer.}

\item{lower, upper}{Names of the bound columns, or \code{NULL}.}
}
\value{
\code{TRUE} when both columns are present and some row spans a non-zero
width, \code{FALSE} otherwise.
}
\description{
The power baselines are estimated one of two ways. A closed-form power has
no Monte Carlo error, and the compute_freq_power() family reports that as a
degenerate interval, \code{c(power, power)}. Drawing those bounds would put a
zero-height error bar - a bare cap tick - through every marker, so a plot
asks this first and adds the layer only when the bounds say something.
}
\details{
Bound columns missing from \code{data} count as no uncertainty: results written
before the bounds were recorded still have to plot.
}
\keyword{internal}
