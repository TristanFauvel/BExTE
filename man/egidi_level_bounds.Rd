% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vectorised_egidi_mixture.R
\name{egidi_level_bounds}
\alias{egidi_level_bounds}
\title{Outer bounds of the region where a two-component normal mixture exceeds a level}
\usage{
egidi_level_bounds(level, weights, means, sds)
}
\arguments{
\item{level}{The density level, one per replicate.}

\item{weights}{A list of the two component weights.}

\item{means}{A list of the two component means.}

\item{sds}{A list of the two component standard deviations.}
}
\value{
A list with \code{lower} and \code{upper}, each one value per replicate.
}
\description{
The conflict p-value integrates the predictive mixture over the set where its
density is at or below the density at the observed statistic. That set is the
complement of a bounded region, and this returns an interval containing it.
}
\details{
A component whose weighted density never reaches \code{level / 2} cannot on its own
carry the mixture above \code{level}, so halving the level makes the bound valid for
the sum rather than for either term: outside the returned interval both
components sit below \code{level / 2} and the mixture below \code{level}.
}
\keyword{internal}
