% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/npp_kl_calibration.R
\name{npp_kl_objective}
\alias{npp_kl_objective}
\title{The KL calibration objective}
\usage{
npp_kl_objective(
  eta,
  theta_target_compatible,
  theta_target_mtd,
  theta_source,
  se_source,
  se_target_expected,
  lambda_kl,
  c_target,
  n_nodes = 80L
)
}
\arguments{
\item{eta}{Length-two numeric vector, \code{log(a)} and \code{log(b)}.}

\item{theta_target_compatible}{Hypothetical estimate under compatibility.}

\item{theta_target_mtd}{Hypothetical estimate at the maximum tolerable
discrepancy.}

\item{theta_source}{Source treatment effect estimate.}

\item{se_source}{Standard error of the source estimate.}

\item{se_target_expected}{Expected standard error of the target estimate.}

\item{lambda_kl}{Weight on the compatible term.}

\item{c_target}{Shape of the two reference Beta distributions.}

\item{n_nodes}{Number of quadrature nodes.}
}
\value{
A single number, possibly \code{Inf}.
}
\description{
Weighted sum of the divergence from the compatible posterior to
\code{Beta(c, 1)}, which concentrates near full borrowing, and from the maximum
tolerable discrepancy posterior to \code{Beta(1, c)}, which concentrates near no
borrowing. The shape parameters are passed on the log scale so the optimiser
works unconstrained in a space where they are automatically positive.

Any non-finite value is reported as \code{Inf} rather than propagated, so that a
shape the quadrature cannot handle ends the line search instead of stopping
the run.
}
\keyword{internal}
