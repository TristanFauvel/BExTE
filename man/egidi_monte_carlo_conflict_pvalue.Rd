% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vectorised_egidi_mixture.R
\name{egidi_monte_carlo_conflict_pvalue}
\alias{egidi_monte_carlo_conflict_pvalue}
\title{Prior-predictive conflict p-value by simulation}
\usage{
egidi_monte_carlo_conflict_pvalue(
  t_obs,
  psi,
  mu_p,
  sigma_p,
  mu_q,
  sigma_q,
  draws = 1000L,
  seed = NULL
)
}
\arguments{
\item{t_obs}{Observed target statistic, a single value.}

\item{psi}{Weight on the weak component, a single value.}

\item{mu_p, sigma_p}{Informative component predictive mean and standard deviation.}

\item{mu_q, sigma_q}{Weak component predictive mean and standard deviation.}

\item{draws}{Number of hypothetical replications.}

\item{seed}{Optional seed, so that a replicate's p-value is reproducible.}
}
\value{
A list with the estimate \code{pvalue} and its \code{standard_error}.
}
\description{
The Monte Carlo fallback of \code{\link[=egidi_normal_conflict_pvalue]{egidi_normal_conflict_pvalue()}}, for
checking the deterministic calculation and for sampling distributions with no
closed-form level sets. Egidi, Pauli and Torelli used 1000 hypothetical
replications; the deterministic route is preferred in the simulation study
because a nested Monte Carlo error would propagate into every operating
characteristic.
}
\details{
The estimator adds one to the numerator and the denominator, which keeps the
p-value away from zero and makes it the usual randomisation-test estimator.
Drawing the component indicator and both component values up front means the
same underlying uniforms serve every candidate weight, so the p-values compared
across candidates differ only through the weight.
}
\keyword{internal}
