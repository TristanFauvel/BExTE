% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_case_study_summary.R
\name{table_drift_ranges_and_sample_sizes}
\alias{table_drift_ranges_and_sample_sizes}
\title{Drift ranges and target sample sizes for each case study (table S1)}
\usage{
table_drift_ranges_and_sample_sizes(case_studies_config_dir, tables_dir)
}
\arguments{
\item{case_studies_config_dir}{Directory holding the case study YAMLs.}

\item{tables_dir}{Directory to write into.}
}
\value{
The path of the written \code{.tex} file.
}
\description{
The paper's table S1, which merges what used to be two
tables: the drift and treatment-effect ranges, and the target-study
sample sizes. The ranges come from \code{\link[=compute_drift_range]{compute_drift_range()}}, the function
the simulation builds its drift grid with, so the table describes the
grid the current configs produce rather than whatever an older results
directory happens to hold.

The sample-size columns are the nominal totals \code{floor(N_S / k)}, as the
paper prints them. The simulation puts \code{floor(N_S / (2k))} patients in
each arm, so the total it actually simulates can be one or two lower -
see \code{\link[=paper_sample_size_per_arm]{paper_sample_size_per_arm()}}.

Every case study is listed whatever the selection, because the paper's
table always has all six rows.
}
